# 系统全局设计规范

> 本章节定义促销中心系统级的设计规范，适用于所有功能页面，是各业务模块（促销活动、模版、队列、终止单等）共用的底层结构与交互基线。

---

## 一、浏览器使用要求

### 设计目标
明确系统运行环境，降低兼容性维护成本，保障渲染与交互一致性。

### 方案探索

| 方案 | 优点 | 缺点 |
|------|------|------|
| A. 仅支持 Chrome | 测试成本最低，新特性可用 | 用户被强绑定单一浏览器 |
| B. Chromium 内核全家桶（Chrome/Edge） | 覆盖主流办公浏览器，维护成本低 | 排除 Firefox/Safari 用户 |
| C. 兼容所有现代浏览器+降级 | 覆盖面最广 | 测试矩阵庞大，降级逻辑复杂 |

### 推荐方案：Chrome 86+（已确认）
- 以 Chrome 86 版本为基准进行开发与测试
- 其他 Chromium 内核浏览器（Edge 86+）通常可用，但不作为主要测试对象
- 明确不支持 IE 及任何"兼容模式"（IE 内核）

### 详细要求

| 项目 | 要求 |
|------|------|
| 推荐浏览器 | Chrome 86+ |
| 可用浏览器 | Edge 86+、Firefox 86+、Safari 14+（尽力兼容，不保证100%） |
| 不支持 | IE 全系列、Chrome/Edge 兼容模式、国产浏览器兼容模式 |
| 屏幕分辨率 | 最低 1366×768；推荐 1920×1080；支持 2K/4K 高分屏缩放 |
| JavaScript | 必须启用 |
| Cookie/LocalStorage | 必须启用（用于保存用户列设置、分屏状态等） |
| 移动端 | 仅支持平板横屏 ≥1024px；手机竖屏不支持（B端系统） |
| 网络 | 建议带宽 ≥2Mbps；弱网下提示"网络较慢" |

---

## 二、页面布局模式

### 设计目标
统一全站视觉骨架，降低用户学习成本，支持不同场景的专注/对比需求。

### 方案探索

| 方案 | 说明 | 评价 |
|------|------|------|
| A. 左侧固定导航 + 顶部栏 + 内容区 | 经典后台布局 | ✅ 信息密度高，菜单层级承载强 |
| B. 顶部水平导航 | 一级菜单少时简洁 | ❌ 本系统菜单多，水平放不下 |
| C. 无侧栏（卡片首页式） | 现代感 | ❌ 不适合频繁切换的业务系统 |

### 推荐方案：左侧导航 + 顶部栏 + 主内容区

### 布局结构

| 区域 | 说明 |
|------|------|
| 左侧导航栏 | 宽度 240px（展开）/ 64px（折叠图标）；支持折叠记忆；最多三级菜单 |
| 顶部栏 | 高度 48px；左侧面包屑（当前位置），右侧用户信息/通知/全屏按钮 |
| 主内容区 | 剩余空间；内含页面标题、工具栏、筛选区、列表/表单 |
| 分屏模式 | 主内容区可左右分栏（列表 + 详情），分割条可拖拽 |
| 全屏模式 | 隐藏左侧导航和顶部栏，主内容区占满浏览器 |

### 交互规则
- 左侧导航折叠状态持久化（localStorage）
- 顶部面包屑自动根据路由生成，支持点击上级返回
- 分屏/全屏可叠加使用
- 主内容区最小宽度 1024px，低于此出现横向滚动条

---

## 三、页面菜单分类

### 设计目标
按业务域归类，层级清晰，菜单深度不超过3级，高频入口易达。

### 菜单层级规范
- 一级菜单：业务大类（数据看板、促销规划等）
- 二级菜单：功能模块（如"促销活动"下的各促销类型分组）
- 三级菜单：具体页面（如"折扣促销"下的商品折扣/时段折扣）
- 菜单深度：最多3级；超过3级必须拆分或合并

### 全站菜单结构（一/二/三级）

| 一级 | 二级 | 三级 |
|------|------|------|
| 数据看板 | — | 档前/档中/档末三态看板 |
| 促销规划 | 促销档期 / 档期商品 | — |
| 促销模版 | 促销模板 | — |
| 促销活动 | 档期智能促销 / 特价促销 / 折扣促销 / 满减满赠 / 满赠券促销 | 具体促销类型页（如商品折扣、购满折扣） |
| 促销活动队列 | 待生效促销队列 / 生效促销列表 / 失效促销档案 / 促销终止单 | — |
| 促销试算 | 模拟算价 | — |
| 促销活动校验 | — | — |
| 促销监控 | 促销分析 / 促销达成 / 促销预警 / 销售明细报表 / 库存明细报表 / 商品排行明细 / 人员订单统计报表 / 门店促销品异常 | — |
| 活动基础设置 | 门店组维护 / 促销组合维护 | — |
| 主档信息 | 商品 / 门店 / 品类 / 品牌 / 供应商 / 柜组 等 | — |
| 系统配置 | 权限 / 字典 / 参数 | — |

### 说明
- 不提供"我的常用"菜单收藏区（已确认）
- 菜单结构固定，不支持用户自定义

---

## 四、页面筛选条件（组合查询规范）

### 设计目标
全站列表页统一筛选交互，降低学习成本，支持高效定位数据。

### 筛选区规范

| 规范项 | 规则 |
|--------|------|
| 位置 | 列表上方，工具栏之下，默认展开 |
| 布局 | 多列网格（响应式：1920宽4列，1366宽2列） |
| 条件关系 | 所有条件 AND（与） |
| 控件类型 | 文本框（模糊）、下拉（精确）、日期范围、日期时间范围 |
| 查询按钮 | 主按钮"查询"；次按钮"重置" |
| 默认值 | 下拉默认"全部"；日期默认空（不限） |
| 触发方式 | 点击"查询"或输入框回车；下拉改变需点"查询" |

### 默认关键字查询入口（已确认）
每个列表页默认提供一个**关键字查询入口**（位于筛选区最前方，醒目位置）：
- 单一文本框，支持对当前页核心字段（如编号、名称/主题）的模糊匹配
- 用户无需展开高级筛选即可快速搜索
- 关键字查询与下方组合查询条件为 AND 关系，可叠加使用
- 不提供"保存常用筛选"功能

### 通用筛选字段（全站列表页必备）

| 字段 | 控件 | 说明 |
|------|------|------|
| 关键字 | 文本 | 默认入口，模糊匹配编号/名称等核心字段 |
| 编号 | 文本 | 模糊匹配主键编号 |
| 名称/主题 | 文本 | 模糊匹配 |
| 状态 | 下拉多选 | 单据状态枚举 |
| 类型 | 下拉单选 | 促销类型/模板类型等 |
| 时间范围 | 日期时间 | 起止两个选择器 |
| 创建人 | 文本 | 模糊 |
| 创建时间 | 日期范围 | — |

---

## 五、页面数据状态与单据数量

### 设计目标
让用户一眼掌握各类单据的当前状态分布，快速发现待处理项。

### 单据状态规范

| 业务对象 | 状态枚举 | 流转 |
|----------|---------|------|
| 促销活动 | 草稿→审核→待生效→生效→（延期/取消生效）→终止 | 单向流转，部分可回退 |
| 促销模板 | 草稿→有效→停用 | — |
| 促销终止单 | 草稿→已审核→已生效 | — |
| 审核单据 | 待审核→已审核/已驳回 | — |

### 数量展示规范

| 位置 | 展示方式 |
|------|---------|
| 列表 Tab | Tab 文字后跟数字角标，如"待审核 (12)" |
| 左侧菜单 | 二级菜单后跟角标，如"待生效促销队列 (5)"，红色背景突出 |
| 列表页头 | 总数汇总，如"共 156 条"，各状态分别统计 |
| 仪表盘 | 各状态卡片化展示数量与趋势 |

### 交互规则
- 角标数字实时更新（列表刷新或操作后同步）
- 数量 >99 显示 "99+"
- 数量 =0 不显示角标（减少视觉噪音）
- 点击带角标的 Tab/菜单自动加该状态筛选

---

## 六、列表配置

### 设计目标
让用户按习惯自定义列表形态，提升数据浏览与处理效率。

### 6.1 全屏展示
- 入口：工具栏"全屏"按钮
- 行为：隐藏侧栏+顶栏，列表占满浏览器
- 退出：Esc 或"退出全屏"
- 状态不持久化

### 6.2 列表高度自适应
用户可调整行高，适应不同屏幕和阅读习惯。

| 方案 | 行高 | 适用 |
|------|------|------|
| 紧凑 | 32px | 数据密集、大屏监控 |
| 标准（默认） | 40px | 日常 |
| 宽松 | 52px | 字段多、需快速扫读 |

- 列表整体高度：默认撑满主内容区可视高度（不出现页面滚动，仅列表内滚动）；可切换"按内容高度"（页面滚动）
- 设置持久化到 localStorage

### 6.3 列表字段配置

| 能力 | 说明 |
|------|------|
| 字段显隐 | 列设置面板勾选，实时生效 |
| 字段排序 | 拖拽列头调整左右顺序 |
| 列宽调整 | 拖拽列头右边框；双击自动适应内容宽度 |
| 固定列（冻结） | 关键列（如序号、操作）可固定左侧不随横向滚动 |
| 列排序状态记忆 | 保存列顺序/宽度/显隐到 localStorage |
| 恢复默认 | 提供"恢复默认列设置"按钮 |

### 6.4 列表行交互
- 行悬停高亮
- 单击行：进入详情（或分屏展开详情）
- 复选框：批量选择，**仅支持当前页内选择，不支持跨页累积**（已确认）；切换页码或查询条件后，之前选中自动清空
- 行内操作：操作列放主要操作，次要操作收进"更多 ▾"

---

## 七、列表分页

### 设计目标
平衡单页数据量与加载性能，支持快速翻页和定位。

### 分页规范

| 项目 | 规则 |
|------|------|
| 默认每页条数 | 20 条 |
| 可选条数 | 10 / 20 / 50 / 100 |
| 分页位置 | 列表底部右侧 |
| 显示信息 | "共 X 条" + "第 Y/Z 页" |
| 翻页控件 | 首页 ‹ 1 2 3 ... 末页 › |
| 跳页 | 输入页码回车跳转 |
| 性能 | 单页 ≤100 条；超过 10 万条强制要求加筛选条件 |

### 交互规则
- 翻页后保留筛选条件和排序状态
- 切换每页条数后回到第1页
- 翻页时清空当前页选中行（因不支持跨页累积）

---

## 八、导入（异步）

### 设计目标
支持大批量数据导入，不阻塞前端操作，过程可追踪，结果可回溯。

### 方案探索

| 方案 | 说明 | 评价 |
|------|------|------|
| A. 同步导入 | 上传后前端等待直到完成 | ❌ 大数据量超时、白屏 |
| B. 异步导入 | 上传后生成任务，后台处理 | ✅ 不阻塞，可查进度 |
| C. 异步+任务中心 | B 的增强，集中管理所有导入任务 | ✅ 体验最好 |

### 推荐方案：异步 + 任务中心（已确认）

### 导入流程

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 下载模板 | 提供标准 CSV/Excel 模板 |
| 2 | 填写并上传 | 选择文件，单文件 ≤10MB，≤1000 行 |
| 3 | 生成导入任务 | 系统返回任务ID，状态=处理中 |
| 4 | 后台异步校验+写入 | 不阻塞前端，用户可继续其他操作 |
| 5 | 完成/失败通知 | 站内消息+右上角通知 |
| 6 | 查看结果 | 任务中心查看成功/失败数，失败可下载错误报告 |

### 失败处理策略（已确认）：部分成功 + 错误报告
- 校验按行进行，合法行正常写入，非法行跳过并记录
- 导入完成后显示"成功 N 条，失败 M 条"
- 失败行生成错误报告（含行号、列名、错误原因），用户下载修正后可对失败行重新导入
- 不做"全部回滚"，避免因个别错误导致整批数据无法导入

### 任务中心
- 入口：顶部栏"任务中心"图标，显示进行中任务数量角标
- 列表字段：任务类型、文件名、状态（处理中/成功/失败）、进度%、提交人、提交时间、完成时间、操作（下载结果/下载错误报告/重试）
- 任务记录保留 30 天

### 说明
- 不提供"导入前预览校验"（已确认）：上传即进入异步处理流程，校验在后台完成

---

## 九、导出（异步）

### 设计目标
支持大数据量导出，前端不卡顿，文件可重复下载。

### 方案探索

| 方案 | 说明 | 评价 |
|------|------|------|
| A. 同步导出 | 点导出直接下载 | ❌ 大数据量浏览器卡死/超时 |
| B. 异步导出 | 提交请求，后台生成，完成下载 | ✅ 稳定 |
| C. 异步+下载中心 | B 的增强，集中管理导出文件 | ✅ 推荐 |

### 推荐方案：异步 + 下载中心（已确认）

### 导出流程

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 设置导出范围 | 当前筛选 / 全部 / 选中行 |
| 2 | 选择导出字段 | 默认导出可见列；可勾选"全字段"导出含隐藏列 |
| 3 | 提交导出请求 | 生成导出任务，状态=生成中 |
| 4 | 后台异步生成 Excel | 不阻塞前端 |
| 5 | 完成通知 | 站内消息+通知 |
| 6 | 下载 | 下载中心点击下载 |

### 导出规格（已确认）

| 项目 | 规则 |
|------|------|
| 导出列 | 默认可见列；支持勾选"全字段"导出含隐藏列 |
| 列顺序 | 当前列表显示顺序（隐藏列追加到末尾） |
| 列头 | 列表表头文字 |
| 数据格式 | 原始值，如日期"2026-07-20 14:30:00" |
| 文件格式 | .xlsx |
| 文件名 | {业务对象}列表_YYYYMMDD_HHmmss.xlsx |
| 单次上限 | 10 万行；超出提示缩小范围 |
| 文件保留时长 | **30 天** |

### 下载中心
- 与任务中心合并（统称"任务中心"，含导入和导出两类任务）
- 列表字段：任务类型（导入/导出）、文件名、状态、进度%、数据条数、提交人、提交时间、完成时间、操作（下载/重新生成/删除）
- 导出文件保留 30 天，到期自动清理并提示

---

## 决策点确认汇总

| # | 决策点 | 最终结论 |
|---|--------|---------|
| 1 | 浏览器版本基准 | Chrome 86+ |
| 2 | "我的常用"菜单收藏区 | 不需要 |
| 3 | 默认关键字查询入口 | 提供（每个列表页默认一个关键字搜索框） |
| 4 | 保存常用筛选 | 不提供 |
| 5 | 跨页批量选择 | 不支持（仅当前页，翻页清空） |
| 6 | 导入失败策略 | 部分成功 + 错误报告 |
| 7 | 导入前预览校验 | 不需要 |
| 8 | 导出文件保留时长 | 30 天 |
| 9 | 导出是否含隐藏列 | 支持勾选"全字段"导出含隐藏列 |
