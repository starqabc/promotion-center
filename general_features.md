# 通用基础功能

## 组合查询

### 功能概述
列表页上方提供组合查询区域，支持用户通过多个条件联合筛选活动数据，各条件之间为"与"（AND）关系。

### 查询条件清单

| 查询条件 | 控件类型 | 匹配方式 | 说明 |
|----------|---------|---------|------|
| 活动编码 | 文本输入框 | 模糊匹配 | 输入活动编号中的部分字符即可匹配 |
| 活动主题 | 文本输入框 | 模糊匹配 | 输入活动名称中的部分字符即可匹配 |
| 促销类型 | 下拉单选 | 精确匹配 | 数据源为当前菜单对应的促销类型，默认"全部" |
| 活动状态 | 下拉单选 | 精确匹配 | 草稿/审核/待生效/生效/延期/终止，默认"全部" |
| 活动开始时间 | 日期时间选择器 | 范围匹配 | 筛选开始时间>=所选值的活动 |
| 活动结束时间 | 日期时间选择器 | 范围匹配 | 筛选结束时间<=所选值的活动 |
| 创建时间 | 日期时间选择器 | 范围匹配 | 起止两个选择器，筛选在此时间段内创建的活动 |
| 创建人 | 文本输入框 | 模糊匹配 | 输入创建人姓名或工号进行匹配 |
| 审核状态 | 下拉单选 | 精确匹配 | 待审核/已审核/已驳回，默认"全部" |

### 操作说明

| 操作 | 行为 |
|------|------|
| 查询 | 收集所有已填查询条件，按AND关系过滤列表数据；翻页回到第1页；查询条件保留在输入框中 |
| 重置 | 一键清空所有查询条件恢复默认值；刷新列表回到初始状态 |

### 交互规则
- 查询条件可单独使用也可组合使用
- 空条件视为不限制（等同于"全部"）
- 回车键在任意查询输入框中触发查询
- 查询结果为空时显示"暂无数据"空状态提示

---

## 列设置

### 功能概述
列表表头右侧提供"列设置"入口，允许用户自定义列表显示的列及其顺序，设置持久化到浏览器本地存储。

### 支持的列清单

| 列名 | 默认显示 | 说明 |
|------|---------|------|
| 序号 | 是 | 行号，不可隐藏 |
| 活动编码 | 是 | 活动唯一编号 |
| 活动主题 | 是 | 活动名称 |
| 促销类型 | 是 | 当前菜单对应的促销类型 |
| 模版名称 | 是 | 关联的促销模版名称 |
| 活动状态 | 是 | 草稿/审核/待生效/生效/延期/终止 |
| 商品范围 | 是 | 单品/品类/品牌的统计描述 |
| 活动开始时间 | 是 | — |
| 活动结束时间 | 是 | — |
| 创建人 | 否 | — |
| 创建时间 | 否 | — |
| 修改人 | 否 | — |
| 修改时间 | 否 | — |
| 审核人 | 否 | — |
| 审核时间 | 否 | — |
| 生效人 | 否 | — |
| 生效时间 | 否 | — |

### 操作说明

| 操作 | 行为 |
|------|------|
| 打开列设置 | 点击工具栏齿轮图标，弹出列设置面板 |
| 显示/隐藏列 | 勾选或取消列名前的复选框，列表实时刷新列显隐 |
| 调整列顺序 | 面板内拖拽列名调整顺序，列表同步更新 |
| 关闭面板 | 点击面板外区域或关闭按钮，保留当前设置 |

### 持久化规则
- 用户列设置保存到浏览器 localStorage
- 下次打开同一页面时自动恢复上次列配置
- 清除浏览器缓存后恢复默认列设置

---

## 刷新

### 功能概述
列表工具栏提供"刷新"按钮，用于重新加载列表数据。

### 操作说明

| 操作 | 行为 |
|------|------|
| 点击刷新按钮 | 重新请求数据源，渲染列表；翻页回到第1页；保留当前查询条件 |
| 浏览器刷新（F5） | 刷新整个页面，等同于首次加载（查询条件可能丢失） |

### 适用场景
- 其他用户新增/修改/删除了活动数据，手动刷新查看最新状态
- 提交审核、审核通过等操作完成后，刷新确认状态变更
- 数据加载异常后手动重试

### 交互规则
- 刷新过程中显示加载态（列表区域loading遮罩）
- 刷新后保留查询条件但不保留翻页位置
- 页面切到后台超过5分钟后切回前台自动刷新一次

---

## 分屏

### 功能概述
支持将列表页分为左右两栏：左侧为活动列表，右侧为选中活动的详情预览，无需离开列表页即可快速查看活动详细信息。

### 布局说明

| 区域 | 默认宽度 | 内容 |
|------|---------|------|
| 左侧 | 约60% | 活动列表（含查询条件、工具栏、表格、分页） |
| 右侧 | 约40% | 选中活动的详情预览（只读模式，按Tab展示） |
| 分割条 | 4px | 可拖拽调整左右宽度，最小200px，最大80% |

### 右侧详情Tab

| Tab | 内容 |
|-----|------|
| 基本信息 | 活动编号、活动主题、促销类型、模版名称、活动状态、开始/结束时间、创建人/时间、审核人/时间等 |
| 门店范围 | 已选门店列表（全部门店提示或部分门店明细表格） |
| 商品范围 | 商品明细列表（商品编码、条码、名称、规格、单位、售价、促销价、折扣等） |
| 承担规则 | 供货商/采购/门店承担比例（如已配置则展示） |

### 操作说明

| 操作 | 行为 |
|------|------|
| 进入分屏 | 点击列表行的"分屏查看"按钮或工具栏"分屏"开关，右侧展开活动详情 |
| 切换活动 | 点击左侧列表其他行，右侧自动切换为对应活动详情 |
| 拖拽分割条 | 左右拖动中间分割条调整左右宽度比例 |
| 退出分屏 | 点击右侧关闭按钮或工具栏"分屏"开关，恢复全宽列表 |

### 交互规则
- 分屏状态下列表选中行高亮（蓝色背景）
- 右侧详情全部只读，不可编辑
- 分屏状态持久化到 localStorage，下次打开自动恢复

---

## 全屏

### 功能概述
点击工具栏"全屏"按钮，列表区域扩展至浏览器全屏，最大化数据展示空间，适合大屏或数据密集场景。

### 操作说明

| 操作 | 行为 |
|------|------|
| 进入全屏 | 点击工具栏"全屏"按钮（或按F11），隐藏侧边导航栏和顶部栏 |
| 退出全屏 | 按Esc键或点击"退出全屏"按钮，恢复原始布局 |

### 交互规则
- 全屏模式下查询条件、工具栏、分页均保留
- 仅隐藏全局导航（侧边栏+顶部栏），列表功能不变
- 全屏状态不持久化，刷新后恢复默认布局
- 全屏与分屏可同时使用

---

## 列表字段排序

### 功能概述
列表表头支持点击排序，用户可按任意可排序列对当前页数据进行升序或降序排列。

### 可排序列

| 列名 | 排序逻辑 |
|------|---------|
| 活动编码 | 字符串字典序（0-9 > A-Z > a-z） |
| 活动主题 | 字符串字典序 |
| 活动状态 | 按状态流转权重排序：草稿(1) < 审核(2) < 待生效(3) < 生效(4) < 延期(5) < 终止(6) |
| 活动开始时间 | 日期时间先后（早→晚 / 晚→早） |
| 活动结束时间 | 日期时间先后 |
| 创建时间 | 日期时间先后 |
| 修改时间 | 日期时间先后 |

### 操作说明

| 点击次数 | 排序方向 | 图标 |
|---------|---------|------|
| 第1次 | 升序（ASC） | ↑ |
| 第2次 | 降序（DESC） | ↓ |
| 第3次 | 取消排序，恢复默认 | — |

### 交互规则
- 默认排序：创建时间降序（最新在前）
- 排序仅对当前加载数据生效（前端排序）
- 同一时间只有一列处于排序状态
- 翻页后排序状态保留

---

## 导入

### 功能概述
工具栏提供"导入"按钮，支持通过Excel/CSV模板批量导入活动数据，系统自动校验并创建活动，校验不通过时下载错误报告。

### 导入流程（5步）

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1-下载模板 | 点击"导入"→"下载导入模板" | 下载对应促销类型的CSV模板文件，含示例数据行 |
| 2-填写数据 | 按模板列填写活动数据 | 每行一个活动；必填列不可为空；日期格式 yyyy-MM-dd HH:mm:ss |
| 3-上传文件 | 点击"导入"→选择文件 | 支持 .csv / .xlsx 格式，单文件最大10MB，最多1000行 |
| 4-系统校验 | 自动逐行校验 | 必填字段、数据格式、编码合法性、时间合理性、重复检查 |
| 5a-校验通过 | 批量创建活动 | 显示"成功导入N个活动"Toast提示，刷新列表 |
| 5b-校验不通过 | 下载错误报告 | 标注出错行号+列名+错误原因，不创建任何活动，修正后重新上传 |

### 模板字段说明

| 列名 | 必填 | 格式/限制 | 示例 |
|------|------|---------|------|
| 活动主题 | 是 | 文本，1-50字符 | 618家清一口价 |
| 促销模版编码 | 是 | 系统中已存在的模版编码 | T-1001 |
| 活动开始时间 | 是 | yyyy-MM-dd HH:mm:ss | 2026-07-20 00:00:00 |
| 活动结束时间 | 是 | 晚于开始时间 | 2026-07-22 23:59:59 |
| 活动说明 | 否 | 文本，0-200字符 | 618大促活动 |
| 门店编码 | 否 | 多门店逗号分隔，空=全部门店 | S001,S002 |
| 商品编码 | 否 | 多商品逗号分隔 | P001,P002 |

### 校验规则

| 校验项 | 错误提示 |
|--------|---------|
| 必填字段为空 | "第N行[列名]不能为空" |
| 日期格式错误 | "第N行[列名]格式错误，应为yyyy-MM-dd HH:mm:ss" |
| 模版编码不存在 | "第N行模版编码在系统中不存在" |
| 开始>结束时间 | "第N行开始时间不能晚于结束时间" |
| 活动编码重复 | "第N行活动编码与第M行重复" |

---

## 导出

### 功能概述
工具栏提供"导出"按钮，支持将当前查询条件下的全部活动数据导出为Excel文件。

### 导出方式

| 方式 | 操作 | 范围 |
|------|------|------|
| 导出当前 | 点击"导出"→"导出当前" | 当前查询条件筛选的全部数据（不受分页限制） |
| 导出全部 | 查询条件为空时导出 | 该菜单下全部活动数据 |
| 导出选中 | 先勾选活动行，点击"导出"→"导出选中" | 仅导出勾选的行 |

### 导出规格

| 项目 | 说明 |
|------|------|
| 导出列 | 当前列表可见列（受列设置影响） |
| 列顺序 | 当前列表显示顺序 |
| 列头 | 列表表头文字 |
| 数据格式 | 原始值，如日期"2026-07-20 14:30:00" |
| 文件格式 | .xlsx（Excel 2007+） |
| 文件名 | 一口价活动列表_YYYYMMDD_HHmmss.xlsx |
| 单次上限 | 100,000条，超出提示缩小范围 |

### 交互规则
- 导出过程中显示进度提示"正在导出，请稍候..."
- 导出完成后自动触发浏览器下载
- 导出失败提示"导出失败，请重试"

---

## 选门店

### 功能概述
在活动新增/编辑的门店范围配置区域提供"选门店"按钮，弹出标准门店选择弹窗，支持多条件筛选和批量选择门店。

### 弹窗布局

| 区域 | 内容 |
|------|------|
| 筛选区 | 区域下拉、价格组下拉、门店编码/名称搜索框、查询/重置按钮 |
| 可选门店列表 | 全选复选框 + 表格（门店编码、门店名称、区域、价格组、业态），分页展示 |
| 底部操作栏 | 全选、确定（回填）、取消按钮 |

### 筛选条件

| 条件 | 控件 | 数据源 | 说明 |
|------|------|-------|------|
| 区域 | 下拉单选 | 门店主档-区域字段去重 | 默认"全部" |
| 价格组 | 下拉单选 | 门店主档-价格组字段去重 | 默认"全部" |
| 门店编码/名称 | 文本输入 | — | 模糊匹配编码或名称 |

### 操作说明

| 操作 | 行为 |
|------|------|
| 打开弹窗 | 点击门店范围区"选门店"按钮 |
| 查询 | 设置筛选条件后点击"查询"，刷新列表 |
| 选择 | 勾选门店前复选框；支持全选当前页 |
| 确定 | 选中门店回填至门店范围明细表，关闭弹窗 |
| 取消 | 关闭弹窗，不保存 |

### 回填后明细表字段
序号、门店编码、门店名称、区域、价格组、业态、操作（移除按钮）

---

## 选商品

### 功能概述
在活动新增/编辑的商品范围配置区域提供"选商品"按钮，弹出标准商品选择弹窗，支持多条件组合查询和批量选择商品。组合价/一口价活动弹窗含组合编码列。

### 弹窗布局

| 区域 | 内容 |
|------|------|
| 查询区 | 活动编码、活动名称、模版名称、促销类型、商品编码/条码/名称、品类（三级联动）、品牌、柜组、组合编码（仅组合价） |
| 列表区 | 全选复选框 + 表格（编码、条码、名称、规格、单位、品类、品牌、售价 + 组合编码列），分页 |
| 底部 | 全选、确定、取消 |

### 查询条件明细

| 条件 | 控件 | 匹配方式 |
|------|------|---------|
| 活动编码 | 文本输入 | 模糊 |
| 活动名称 | 文本输入 | 模糊 |
| 模版名称 | 文本输入 | 模糊 |
| 促销类型 | 下拉单选 | 精确 |
| 商品编码/条码/名称 | 文本输入 | 三字段联合模糊 |
| 品类 | 下拉三级联动 | 精确（大类→中类→小类） |
| 品牌 | 下拉单选 | 精确 |
| 柜组 | 下拉单选 | 精确 |
| 组合编码 | 下拉单选 | 精确（仅组合价/一口价弹窗） |

### 商品列表列
复选框、商品编码、商品条码、商品名称、规格、单位、品类、品牌、售价 + 组合编码（组合价/一口价含）

### 回填后明细表字段
- 普通活动：序号、商品编码、商品条码、商品名称、规格、单位、售价、促销进价、折扣、限购数量
- 组合价活动：序号、组合编码、商品编码、商品条码、商品名称、商品数量、商品简称、规格、单位、售价、参考进价
- 支持行内+/-按钮追加/删除商品行
